class Solution {
public:
    bool uniformArray(vector<int>& nums1) 
    {
        //If all numbers are even → choose nums2[i] = nums1[i].If there is at least one odd number:Keep the odd number(s) as odd.For every even number, subtract any odd number → even - odd = odd.
        return true;
    }
};